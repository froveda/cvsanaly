<%= form_for(@tag_revision) do |f| %>
  <% if @tag_revision.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_revision.errors.count, "error") %> prohibited this tag_revision from being saved:</h2>

      <ul>
      <% @tag_revision.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %><br />
    <%= f.number_field :tag_id %>
  </div>
  <div class="field">
    <%= f.label :commit_id %><br />
    <%= f.number_field :commit_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
